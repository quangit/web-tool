---
import FileHashPage from '../../components/FileHashPage.astro';
const title = "KMAC256 File Hash";
const description = "Calculate KMAC256 hash from file. Free online KMAC256 checksum calculator.";
const algorithmName = "KMAC256";
const jsFile = "https://cdnjs.cloudflare.com/ajax/libs/js-sha3/0.9.3/sha3.min.js";
const methodWrapper = "withOptions(kmac256, ['bits', 'key'])";
const activePage = "hash/kmac256_file_hash";
---
<FileHashPage 
    title={title}
    description={description}
    algorithmName={algorithmName}
    jsFile={jsFile}
    methodWrapper={methodWrapper}
    activePage={activePage}
/>
